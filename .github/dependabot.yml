# Dependabot configuration
# Note: docker and github-actions are managed by Renovate (renovate.json)
# which provides more advanced features like automerge and vulnerability alerts.
# Keep Dependabot for any future package ecosystems not covered by Renovate.
version: 2
updates: []
